var start = clock();
print start;
print typeof(start);

var count = 0;
for (var i = 0; i < 1000000; i=i+1) {
    count = count+1;
    count = count / 2;
}

var delta = clock() - start;
print typeof(delta);
print "used " + delta + "ms";